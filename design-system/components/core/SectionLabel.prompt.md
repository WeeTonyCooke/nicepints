Uppercase section divider — tracked label, hairline `--line` rule, optional right-aligned count. Sets the quiet editorial rhythm between feed sections.

```jsx
<SectionLabel count="12 pints">Latest</SectionLabel>
```
