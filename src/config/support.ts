export const SUPPORT_EMAIL =
  import.meta.env.VITE_SUPPORT_EMAIL?.trim() || 'hello@nicepints.com';

export const DATA_CONTROLLER_NAME =
  import.meta.env.VITE_DATA_CONTROLLER_NAME?.trim() || 'NicePints';
